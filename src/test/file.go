// ini config read
package main

import (
    "flag"
    "fmt"
)

var (
    configFile = flag.String("configfile", "config.ini", "General configuration file")
)
vat TOPIC = make(map[string]string)
func main() {
    
}
